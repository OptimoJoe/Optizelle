project(core)

# Set what interfaces we're using
set(interfaces "cpp" "")

# Add all of our unit tests
compile_add_unit(three_vs "${interfaces}")
compile_add_unit(qn_cauchy "${interfaces}")
compile_add_unit(qn_local_min "${interfaces}")
compile_add_unit(qn_newton "${interfaces}")
compile_add_unit(qn_cauchy_tr "${interfaces}")
compile_add_unit(qn_dogleg_tr "${interfaces}")
compile_add_unit(qn_newton_fail "${interfaces}")
compile_add_unit(qn_newton_tr "${interfaces}")
compile_add_unit(qn_feasible "${interfaces}")
compile_add_unit(qn_cauchy_safe "${interfaces}")
compile_add_unit(qn_dogleg_safe "${interfaces}")
compile_add_unit(qn_newton_safe "${interfaces}")
compile_add_unit(qn_cauchy_tr_safe "${interfaces}")
compile_add_unit(qn_dogleg_tr_safe "${interfaces}")
compile_add_unit(qn_newton_tr_safe "${interfaces}")
compile_add_unit(qn_dogleg_tr_to_cauchy_safe "${interfaces}")
compile_add_unit(qn_dogleg_tr_cauchy_unsafe "${interfaces}")
compile_add_unit(qn_newton_cauchy_unsafe "${interfaces}")
compile_add_unit(qn_newton_tr_cauchy_unsafe "${interfaces}")
compile_add_unit(nsp_basic "${interfaces}")
compile_add_unit(nsp_already_in_nullspace "${interfaces}")
compile_add_unit(nsp_zero "${interfaces}")
compile_add_unit(nsp_projection_is_zero "${interfaces}")
