project(linear_algebra)

# Set what interfaces we're using
set(interfaces "cpp" "")

# Add all of our unit tests
compile_add_unit(gmres_full "${interfaces}")
compile_add_unit(gmres_left_preconditioner "${interfaces}")
compile_add_unit(gmres_restart "${interfaces}")
compile_add_unit(gmres_right_preconditioner "${interfaces}")
compile_add_unit(tcg_basic "${interfaces}")
compile_add_unit(tcg_cp "${interfaces}")
compile_add_unit(tcg_nullspace_solve "${interfaces}")
compile_add_unit(tcg_tr_stopping "${interfaces}")
compile_add_unit(tcg_tr_stopping_with_offset "${interfaces}")
compile_add_unit(tcg_nonsymmetric_preconditioner "${interfaces}")
compile_add_unit(tcg_nonprojector_preconditioner "${interfaces}")
compile_add_unit(tcg_loss_of_orthogonality "${interfaces}")
compile_add_unit(tcg_obj_increase "${interfaces}")
compile_add_unit(tcg_offset_violates_tr "${interfaces}")
compile_add_unit(tcg_offset_violates_safeguard "${interfaces}")
compile_add_unit(tcg_negative_curvature "${interfaces}")
compile_add_unit(tcg_cp_negative_curvature "${interfaces}")
compile_add_unit(tcg_nan_preconditioner "${interfaces}")
compile_add_unit(tcg_nan_operator "${interfaces}")
compile_add_unit(tcg_nonsymmetric_operator "${interfaces}")
compile_add_unit(tcg_negative_curvature_final_safe "${interfaces}")
compile_add_unit(tcg_negative_curvature_last_safe "${interfaces}")
compile_add_unit(tcg_negative_curvature_last_not_safe "${interfaces}")
compile_add_unit(tcg_too_many_failed_safeguard "${interfaces}")
compile_add_unit(tcg_cp_negative_curvature_safeguard "${interfaces}")
compile_add_unit(tcg_cp_safeguard "${interfaces}")
